name: "🚀 Release VO-SE PRO (Official Edition)"

on:
  push:
    branches:
      - main
    tags:
      - 'v*'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build:
    name: Build for ${{ matrix.os }}
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [windows-latest, macos-latest]

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      # --- 1. Python環境構築 ---
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'
          cache: 'pip'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip setuptools wheel
          pip install PySide6 numpy sounddevice soundfile mido janome Pillow pyinstaller
          pip install pyopenjtalk --only-binary :all: || pip install pyopenjtalk

      # --- 2. 公式音源パッキング ---
      - name: Pack Official Voice Data
        run: python modules/tools/pack_voice.py

      # --- 3. C++ エンジンビルド ---
      - name: Build C++ Engine
        shell: bash
        run: |
          mkdir -p bin
          if [ "${{ matrix.os }}" = "windows-latest" ]; then
            g++ -std=c++17 -shared -fPIC -o bin/vose_core.dll src/vose_core.cpp src/world/*.cpp \
              -I. -Isrc -Iinclude -Iinclude/world -O2 -static-libgcc -static-libstdc++
          else
            clang++ -std=c++17 -shared -fPIC -o bin/vose_core.dylib src/vose_core.cpp src/world/*.cpp \
              -I. -Isrc -Iinclude -Iinclude/world -undefined dynamic_lookup -O2
          fi

# --- 4. 実行ファイル一体化 (VO-SE PRO 最適化ビルド) ---
      - name: Package App
        shell: bash
        run: |
          rm -rf dist build dist_out
          mkdir dist_out

          if [ "${{ matrix.os }}" = "windows-latest" ]; then
            # --collect-all をやめて、必要なライブラリだけを指定
            pyinstaller --noconsole --onefile --clean \
              --hidden-import=PySide6.QtCore \
              --hidden-import=PySide6.QtGui \
              --hidden-import=PySide6.QtWidgets \
              --add-data "bin/vose_core.dll;bin" \
              --add-data "assets;assets" \
              --name "VO-SE_PRO_Win" main.py
            
            if [ -f "dist/VO-SE_PRO_Win.exe" ]; then
              cp dist/VO-SE_PRO_Win.exe dist_out/
            else
              exit 1
            fi
          else
            # Mac版も最適化
            pyinstaller --noconsole --windowed --clean \
              --hidden-import=PySide6.QtCore \
              --hidden-import=PySide6.QtGui \
              --hidden-import=PySide6.QtWidgets \
              --add-data "bin/vose_core.dylib:bin" \
              --add-data "assets:assets" \
              --name "VO-SE_PRO_Mac" main.py
            
            if [ -d "dist/VO-SE_PRO_Mac.app" ]; then
              cd dist && zip -r ../dist_out/VO-SE_PRO_Mac.zip VO-SE_PRO_Mac.app && cd ..
            else
              exit 1
            fi
          fi

      # --- 5. 成果物の存在を「物理的」に確認し、なければ強制終了 ---
      - name: 🔍 Final Physical Check
        shell: bash
        run: |
          echo "=== Scanning for Artifacts ==="
          if [ "${{ matrix.os }}" = "windows-latest" ]; then
            if [ -f "dist/VO-SE_PRO_Win.exe" ]; then
              cp dist/VO-SE_PRO_Win.exe dist_out/
              echo "✅ Windows EXE found and moved."
            else
              echo "❌ ERROR: Windows EXE was NEVER created!"
              ls -R
              exit 1
            fi
          else
            if [ -d "dist/VO-SE_PRO_Mac.app" ]; then
              cd dist && zip -r ../dist_out/VO-SE_PRO_Mac.zip VO-SE_PRO_Mac.app && cd ..
              echo "✅ Mac ZIP created."
            else
              echo "❌ ERROR: Mac App was NEVER created!"
              ls -R
              exit 1
            fi
          fi

      # --- 6. アップロード (パスの不一致を完全に防ぐ) ---
      - name: Upload Release
        uses: softprops/action-gh-release@v2
        if: success()
        with:
          tag_name: latest
          name: "VO-SE PRO (Official Edition)"
          # 'overwrite_files' の代わりに 'fail_on_unmatched_files: false' を使ってエラーを回避
          fail_on_unmatched_files: false
          files: |
            dist/VO-SE_PRO_Win.exe
            dist_out/VO-SE_PRO_Win.exe
            dist_out/VO-SE_PRO_Mac.zip
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      # --- [追加] Releaseとは別に、Actions画面に直接ファイルを出す ---
      - name: 📦 Save Artifacts (Direct Download)
        uses: actions/upload-artifact@v4
        with:
          name: VO-SE-PRO-Package-${{ matrix.os }}
          path: dist_out/


          
